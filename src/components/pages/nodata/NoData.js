import './nodata.sass'

const NoData = () => {
    return (
        <div className="no-data">
            <h2>No metrics found for the last 30 mins</h2>
        </div>
    );
}

export default NoData;